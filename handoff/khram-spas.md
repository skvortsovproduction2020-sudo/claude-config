# Handoff — Храм Спас на Каменке (khram-spas)

## 🎯 ЦЕЛЬ ПРОЕКТА
Веб-приложение для прихода: расписание богослужений, запись прихожан, роли для всего персонала, уведомления через Telegram и Max, православный календарь.

## 📍 ТЕКУЩЕЕ СОСТОЯНИЕ
*Обновлено: 2026-06-16 (сессия 38)*

- [x] Next.js приложение на Railway (`npm start`)
- [x] 23+ роли: abbot, admin, priest, deacon, altarnik, senior_altarnik, regent, secretary, florist, candle_seller, cleaner, cook, security, it_staff, developer, moderator, **staff** + функциональные
- [x] role_settings в Supabase (permissions + notifications per role)
- [x] Telegram-интеграция (webhook + notify + inline кнопки)
- [x] **Max-интеграция — РАБОТАЕТ** (токен прописан, вебхук зарегистрирован)
  - Бот: «Календарь прихода», username `@id7813204684_bot`
- [x] Православный календарь (lib/orthodox-calendar.ts + sync-calendar.mjs)
- [x] Умный авто-назначение священника: duty / trebny_main / trebny_small
- [x] Статусы украшений (флорист) + хора (регент) в Telegram и на сайте
- [x] Страница флориста `/florist/schedule`
- [x] Священник видит требы по дежурству (не только назначенные)
- [x] Вход по номеру телефона — **OTP через Telegram или Max** (без SMS/Twilio) ✅
- [x] Благословения: анонимность, группировка по ролям в dropdown
- [x] Роли по отделам в UI: **аккордеон (все секции свёрнуты по умолчанию)**
- [x] `/admin/import` — импорт **✅ ВЫПОЛНЕН** (37 сотрудников в базе)
- [x] `/admin/access` — таблица доступов + отправка настоятелю (1 сообщение/чел)
- [x] `/guide` — публичная инструкция по входу
- [x] Online/Offline точка в StaffManager (🟢 < 5 мин / ⚫ офлайн)
- [x] **Множественные доп. роли для священников** — колонка `extra_roles text[]`
- [x] **Имя настоятеля в /book** — динамически из БД (не hardcode)
- [x] **Дубли уведомлений исправлены** — идемпотентность для callback и cron
- [x] Поле `temple` в богослужениях (⛪ Большой / 🕍 Малый храм)
- [x] Задачи: «↩️ Вернуть исполнителю» + «📅 Продлить срок»
- [x] **Замочек слотов (block_reason)** — при блокировке появляется поле «Причина» (необязательное), причина видна администратору в SlotsModal; SQL: `ALTER TABLE rite_slots ADD COLUMN block_reason TEXT`
- [x] **Заблокированные слоты в /book** — показываются серыми «Запись недоступна» (некликабельно), не скрываются; работает для крещения, венчания, отпевания
- [x] **Страница /admin/test-roles** — переключение роли тест-аккаунта (79111888877@khram-spas.ru); ссылка «Тест-роли» в навигации расписания (desktop + мобильная)
- [x] **Цвета дней в расписании /book** — воскресенье (синий), Пасха (бордовый), Господские праздники (тёмно-синий), Богородичные (голубой), Великие (золотой), суббота (лёгкий синий)
- [x] **Баннер поста в /book** — автоматически показывает Великий/Петров/Успенский/Рождественский пост + дату окончания; `getCurrentFast()` в lib/orthodox-calendar.ts
- [x] **Записки «О здравии» и «О упокоении»** — новый аккордеон-блок 🕯 в /book, форма с именами, таблица `church_notes`, уведомление abbot+deacon+moderator+admin
- [x] **Кабинет прихожанина /cabinet** — регистрация/вход по телефону через OTP (TG или Max) или код на экране; история записей и записок; привязка TG/Max через `/start P_TOKEN`; кнопка «Кабинет прихожанина» на /book
- [x] **Расширенные записки** — 7 типов: О здравии, О упокоении, Сорокоуст, Годовое поминовение, Молебен, Панихида, Особое прошение (5 видов); выбор даты подачи; получатели — алтарный блок (abbot/priest/deacon/altarnik/senior_altarnik)
- [x] **Цвета праздников /book** — воскресенье и двунадесятые Господские (бордовый), богородичные (голубой), великие святые (зелёный); постные дни (ср/пт) — фиолетовая метка «пост» (isFastingDay, сплошные седмицы учтены)
- [x] **Алтарная рассылка записок в крон** — накануне каждой Литургии алтарный блок получает сводку всех pending записок по типу
- [x] **Псалтирь: конфликты с требами** — psalter_manager получает уведомление при создании/подтверждении требы на дату с чтецами
- [x] **Псалтирь: внешняя форма `/psalter`** — прихожане записываются без авторизации; кафизма назначается автоматически
- [x] **Чекбокс «Не отображать в публичном расписании»** — колонка `is_staff_only` в services; фильтр в loadUpcomingPublicServices
- [x] **Убраны роли клира**: Панихида, Молебен, Дежурство после службы из формы богослужения
- [x] **Фикс баг saveService** — ошибки Supabase теперь отображаются в форме
- [x] **Православный календарь редизайн** — авто-открытие сегодня, убраны Чтения, новые цвета праздников (Пасха/Двунадесятые/Великие)
- [x] **Формат ФИО: Фамилия Имя Отчество** — плейсхолдеры унифицированы во всех формах
- [x] **Вид треб как Календарь** — Google Calendar-style, drag-and-drop для `schedule_editor`+
- [x] **UX: Доп. роли без скролла** — список чекбоксов раскрыт полностью
- [x] **Групповые поручения: идемпотентность** — таблица `task_acks(task_id, acknowledger_id, UNIQUE)`, 1 человек = 1 нажатие, «Уже принято» при повторе
- [x] **extra_roles → пункты меню** — `getEffectivePermissions` принимает `string[]`, страницы передают `canTasks`/`canStaff`/`canSlots` в ScheduleView
- [x] **Кнопка «📋 Требы»** — прямо в навигационной полосе расписания (не в гамбургер-меню)
- [x] **Обратные уведомления — групповые поручения** — при каждом «Принято» настоятель+модераторы получают «✅ Имя принял (2/5)»; при 100% — «✅ Все приняли»
- [x] **Кнопка «❌ Не могу выполнить»** — вторая строка кнопок в индивидуальных поручениях (Telegram + Max); поручение возвращается в очередь, admin получает уведомление
- [x] **РЕДИЗАЙН /book — ВЫПОЛНЕН** (сессия 6)
- [x] **ЕДИНЫЙ КАЛЕНДАРЬ ТРЕБ** — фильтры Все/Богослужения/Требы, подфильтры, верификационные точки 🔴🟡🟢 (сессия 7)
- [x] **ПСАЛТИРЬ В КАЛЕНДАРЕ** — фиолетовые блоки 📖, конфликт ⚠ (сессия 7)
- [x] **ПРИЁМНАЯ НАСТОЯТЕЛЯ** — заявки от сотрудников и прихожан, подтверждение с уведомлением (сессия 7)
- [x] **УВЕДОМЛЕНИЯ ПРИХОЖАНИНУ** — при подтверждении/исполнении требы священником (сессия 7)
- [x] **ФИКс MAX-ПРИВЯЗКИ** — regex расширен, свечница может привязать бот (сессия 7)
- [x] **РЕЖИМ ПРОСМОТРА "КАК СОТРУДНИК"** — кнопка 👁 в каждой карточке /admin/staff, золотой баннер, кнопка «← Выйти» (сессия 8)
- [x] **UserRole тип расширен** — добавлен `staff` в types/index.ts (сессия 8)
- [x] **Исправлены TS-ошибки** — ScheduleView (дублирование useState/const), tasks.ts (ok: false) (сессия 8)
- [x] **ПАНЕЛЬ /office — ВЫПОЛНЕНА** — внутренний кабинет записи на таинства и требы (сессия 11)
- [x] **Переход из православного календаря на расписание** — кнопка «Перейти на эту неделю →» в CalendarModal (сессия 28)
- [x] **Цвета праздников v2** — Пасха+Двунадесятые (красный автоматически), Великие (розовый вручную abbot/moderator), колонка `is_custom_feast` в church_calendar (сессия 28)
- [x] **Визуальные записки** — форма записки как бумажная церковная записка: крест SVG + линии с именами, лимит 10 имён (сессия 32)
- [x] **Карточки дней в /book** — каждый день в карточке с цветной шапкой (зелёный/бордовый/фиолетовый), текст поста в шапке (сессия 32)
- [x] **Бот-меню /pominovenie** — команда для клира в Telegram и Max: кнопки по типам, сгруппированный список имён (сессия 32)
- [x] **Настоятель в списке священников** — `is_active = true` в таблице priests для иерея Серафима Дейнэса; теперь появляется в «+ Назначить священника» (сессия 33)
- [x] **Кнопка «↩️ Вернуть в работу»** — из раздела «Выполнено» можно вернуть таинство в статус `confirmed`; кнопка заметная, полноценная (сессия 33)
- [x] **Клик по карточке в календаре** — при нажатии на требу в виде «Календарь» открывается панель: назначить священника, сменить статус, вернуть из выполненных; после изменения календарь перезагружается (сессия 33)
- [x] **Беседа перед венчанием — дежурный священник** — берётся priest_id из выбранного duty-слота, а не по очереди duty_queue_index (сессия 36)
- [x] **Записи прихожан — Просроченные** — кнопка «⚠ Просроч. (N)» с пульсацией, по умолчанию только актуальные (сессия 36)
- [x] **Календарь в Записях** — доступен всем ролям (не только 5 ролям), drag-and-drop только у admin/abbot/moderator (сессия 36)
- [x] **Текст подтверждения беседы** — дата/время + телефон лавочки +7 (921) 341-88-88 (сессия 36)
- [x] **Страница крещения /book — редизайн** — блоки: "Перед крещением" + даты-чипы + "О Таинстве" (раскрывается) + "Что нужно" со Символом Веры (раскрывается) + украшение купели (сессия 36)
- [x] **Крещение /book — финальная полировка** — золотые SVG-иконки вместо emoji, фото купели, полный Символ Веры (12 членов с ударениями), 5 абзацев "О Таинстве", нумерация 1/2/3/6/7, убрана форма записи, заголовок "Вступление в Церковь Христову" (сессия 37)
- [x] **OfficeView — фильтр «⚠ Просроч.»** — кнопка показывает pending/confirmed с прошедшей датой; красный стиль; `upcomingCount` исправлен (`> today`) (сессия 37)
- [x] **BookingsModal — редизайн шапки** — заголовок маленький над кнопками, кнопки на второй строке; убрана логика showPast (была добавлена в 36 ошибочно) (сессия 37)
- [x] **loadDutySlots расширен до 2 месяцев** — беседа перед венчанием показывает даты на 2 месяца вперёд (сессия 36)
- [x] **ДИНАМИЧЕСКИЕ СЛОТЫ ПО ДЕЖУРСТВУ** — прихожанин выбирает конкретное время в confession/service/wedding_slots (сессия 35):
  - `loadDutyTimeSlots(date, period_start, period_end)` → 30-мин слоты внутри периода дежурства, проверяет конфликты с `bookings`
  - `FULL_SLOT_TYPES` (wedding_talk, baptism, wedding, funeral) = 60 мин, HH:00 только, `avail_full`
  - Half-slot (confession, conversation, moleben, panikhida, blessing_car) = 30 мин, HH:00 и HH:30, `avail_half`
  - `confession_form` и `service_form` — выбор дня из списка дежурств → раскрытие → выбор точного времени
  - `wedding_slots` — клик по дню раскрывает inline-слоты (только HH:00), клик по времени → wedding_form с точным временем
  - `createConfessionBooking` и `createTempleBooking` принимают `scheduled_time?: string`
- [x] **Отец Серафим добавлен в Railway** — email `doines48@gmail.com` приглашён как Member в проект `caring-endurance` через GraphQL API (сессия 35)
- [x] **ПРЕДВЕНЧАЛЬНАЯ БЕСЕДА** — полный редизайн раздела «Венчание» в /book (сессия 34):
  - `wedding_slots`: текст «Перед венчанием проводится индивидуальная беседа», показывает ближайшие 2 недели, только `rite_type = wedding_talk` слоты, без деления по храмам
  - `wedding_form`: блоки жених/невеста (ФИО, дата рождения, вероисповедание, № брака), свидетельство о браке (необязательно), желаемая дата венчания из канонических дат (select, 6 мес.), пожелания (хор, оформление, фотограф храма)
  - `createWeddingTalkBooking`: атомарное бронирование слота + structured notes + уведомление дежурного священника и wedding_responsible
  - SlotsModal: badge «✦ Беседа» + кнопка ✦ для пометки individual-слотов как `wedding_talk`
  - `candle_seller`: добавлены `bookings_view: true`, `schedule_view: true`
- [x] **Исправлен church_calendar** — 8 сентября 2026 (Сретение Владимирской иконы) исправлено с `great_feast` на `weekday` — не двунадесятый праздник (сессия 33)

## 🗂 РЕДИЗАЙН /book (сессия 6) — детали

### Новая структура страницы
Главное меню — 5 аккордеон-блоков + расписание богослужений сверху:

| Блок | Что внутри |
|------|-----------|
| ✦ Таинства | Крещение (четверги 18:50) · Венчание (авто-беседа) |
| ⛪ Требы в храме | Отпевание · Молебен · Панихида · Освящение машины · Исповедь · Беседа |
| 🏠 Пригласить священника | Причастие · Соборование · Выезд к больному · Освящение кв. · Панихида на кладбище · Выездное отпевание |
| 📖 Псалтирь | Ссылка на /psalter |
| ✉️ Приёмная настоятеля | Телефон + email + обращение + встреча |

### Новые роли в priests.extra_roles
| Роль | Кто | Назначение |
|------|-----|-----------|
| `baptism_responsible` | диакон Иоанн Дейнес | уведомление при записи на крещение |
| `wedding_responsible` | иерей Владислав Уличев | контроль назначения бесед |
| `funeral_responsible` | иерей Алексий Сергеев | уведомление при записи на отпевание |
| `services_moderator` | иерей Алексий Сергеев | исповедь, беседы, молебны, панихиды, освящение машин |
| `offsite_responsible` | настоятель (через role='abbot') | выездные требы |

### Алгоритм очереди бесед (венчание)
- Поле `duty_queue_index INT` в таблице `priests`
- Следующий священник = наименьший `duty_queue_index` среди `rank='Иерей'`
- Слоты = дни дежурства + все сб/вс
- После записи → `duty_queue_index + 1`
- Принцип: кто ведёт беседу — тот совершает Венчание

### Маршрутизация треб
| Тип | Куда |
|-----|------|
| Крещение | → `baptism_responsible` (диакон Иоанн), четверг 18:50 |
| Венчание | → очередной иерей + `wedding_responsible` для контроля |
| Отпевание | → `funeral_responsible` + admins |
| Молебен/Панихида/Освящение машины | → `services_moderator` |
| Исповедь/Беседа (20/40 мин) | → `services_moderator` |
| Выезды (все типы) | → настоятель (abbot) |

### Контакты приёмной
- 📞 +7 (905) 211-88-81
- ✉️ reception@krestovozdvizhenie.ru

## 📱 ТЕЛЕФОНЫ В БАЗЕ (привязаны к TG/Max)
| Профиль | Телефон | TG | Max |
|---------|---------|-----|-----|
| иерей Серафим Дейнес (abbot) | +79673412888 | ✅ | ✅ |
| иерей Алексий Бакулин (priest) | +79119534056 | ✅ | ✅ |
| диакон Иоанн Дейнес (deacon) | +79956057412 | ✅ | — |
| Тестовый акк / developer | +79111888877 | ✅ | — |

**Без телефона (вход только по email+пароль):**
иерей Владислав Уличев (admin), иерей Алексий Сергеев, иерей Игорь Лещинский, Жанна Кикиа (regent), Олеся Афанасова (florist)

## ⚠️ АНТИПАТТЕРНЫ — ИЗВЕСТНЫЕ БАГИ

| Ситуация | Что случилось | Решение |
|----------|---------------|---------|
| Настоятель не заходит по телефону | `is_archived = true` в profiles | SQL: `UPDATE profiles SET is_archived = false WHERE role = 'abbot'` |
| OTP код приходит, но «Ошибка входа» | `banned_until` в auth.users (≈2036) | SQL: `UPDATE auth.users SET banned_until = NULL WHERE id = '...'` |
| **Архивирование ставит БАН в auth.users** | `archiveStaff` делает: `is_archived=true` + `ban_duration:'87600h'` | При ручном восстановлении через SQL нужно исправлять **оба**: profiles И auth.users |
| «Выполнено» не работает после «Принято» | Идемпотентность блокировала action='done' при status='accepted' | Раздельные if для accept и done в tasks.ts |
| Max кнопки показывают `...` | `answerCallback('...')` вызывался ДО хендлера | Убрать ранний вызов из max/webhook/route.ts |
| `verifyOtp` падает в браузере | `@supabase/ssr` требует server-side для установки кук | Вызывать через route handler `/api/auth/otp-verify` |
| Флорист отображается в «Алтарники» | Попала в таблицу `priests` без ранга | `DELETE FROM priests WHERE profile_id = '...'` |
| Свечница не может войти через Max OTP | `max_chat_id` не заполнен в профиле | Она должна сначала привязать бота кодом `/start [код]` |

## 📁 КЛЮЧЕВЫЕ ФАЙЛЫ
```
C:\Users\89117\khram-spas\

app/book/page.tsx                  — страница /book (5 аккордеон-блоков)
app/actions/bookings.ts            — createBaptismBooking, createWeddingBooking,
                                     createConfessionBooking, getNextWeddingCounselor,
                                     incrementWeddingCounselorQueue, notifyPriestByRole
components/booking/BookingView.tsx — главный компонент /book (2200+ строк)
  Шаги: menu, baptism_form, wedding_slots/form, funeral_form,
        service_form, confession_form, home/home_form,
        abbot_reception/appeal/meeting, schedule, done

app/login/page.tsx                 — вход (телефон + OTP через TG/Max, fallback логин+пароль)
app/actions/otp-login.ts           — requestLoginOtp + verifyLoginOtp
app/api/auth/otp-verify/route.ts   — GET ?token_hash=XXX → verifyOtp server-side → /dashboard
app/actions/tasks.ts               — createTask, assignTask, handleTaskCallback, handleTaskCallbackMax
                                     handleTaskDecline, handleTaskDeclineMax, acknowledgeGroupTask
app/actions/psalter.ts             — loadPsalterWeek, savePsalterReading, checkAndNotifyPsalterConflict
                                     loadPsalterPublicSlots, createPublicPsalterSignup
app/actions/role-settings.ts       — getEffectivePermissions(role, extraRoles: string[])
app/admin/schedule/page.tsx        — читает extra_roles, передаёт canTasks/canStaff/canSlots
app/dashboard/page.tsx             — touchLastSeen, redirect по роли, учитывает extra_roles
app/admin/staff/page.tsx           — панель сотрудников
app/admin/access/page.tsx          — таблица доступов + отправка настоятелю

app/api/telegram/webhook/route.ts  — обработчик: group_ack, task_accept/done/decline, booking_*, deco_*, choir_*
app/api/max/webhook/route.ts       — то же для Max
app/api/max/set-webhook/route.ts   — GET ?secret=CRON_SECRET → регистрирует вебхук Max

app/psalter/page.tsx               — публичная страница записи на чтение (без авторизации)

components/booking/BookingView.tsx — форма записи прихожан (5 блоков)
components/booking/BookingsModal.tsx         — список треб + переключатель Список/Календарь
components/booking/BookingsCalendarView.tsx  — Google Calendar вид (drag-and-drop)
components/psalter/PsalterPublicView.tsx     — форма публичной записи на псалтирь
components/schedule/PsalterModal.tsx         — внутренний редактор чтецов
components/admin/StaffManager.tsx            — аккордеон секций; PriestEditCard (доп. роли без скролла)
components/schedule/ScheduleView.tsx         — canTasks/canStaff/canSlots props, кнопка «📋 Требы»
components/schedule/TasksModal.tsx           — благословения: dropdown с <optgroup> по ролям

lib/notify.ts                      — двойной отправщик Telegram+Max
lib/role-defaults.ts               — DEFAULT_ROLE_PERMISSIONS (включая 5 новых ролей routing)
lib/notification-router.ts         — notifyByEvent (дедупликация по chat_id)
lib/max.ts                         — Max API (Authorization: TOKEN без Bearer)
lib/task-group-defs.ts             — TASK_GROUP_DEFS (вынесен из use server файла)
lib/booking-constants.ts           — BOOKING_TYPE_LABELS, PERSONAL_TYPES, TEMPLE_TYPES, HOME_TYPES
```

## 🔧 ОКРУЖЕНИЕ
- **Деплой**: Railway, проект `caring-endurance`, сервис `khram-spas`
- **URL**: https://khram-spas-production.up.railway.app
- **БД**: Supabase, project `brtkdmrktzukmrokkihg`
- **Ключи**: `C:\Users\89117\.claude\secrets\khram-spas.env`
- **Telegram бот**: @HramSpasBot
- **Max бот**: @id7813204684_bot («Календарь прихода»), токен в Railway `MAX_BOT_TOKEN`
- **ВАЖНО**: НЕ запускать локально — только Railway
- **Деплой через**: `railway up --detach`
- **GitHub**: https://github.com/skvortsovproduction2020-sudo/khram-spas (ветка master, работает)

## 👥 РОЛИ В СИСТЕМЕ

### Духовенство
`priest` `deacon` `altarnik` `senior_altarnik`

### Администрация
`abbot` `admin` `moderator` `secretary` `developer`

### Хор
`regent`

### Службы
`florist` `candle_seller` `cleaner` `cook` `security` `it_staff`

### Без точной роли
`staff` — матушка, казначей, бухгалтер, организатор, рабочий (5 чел)

### Функциональные
`baptism_priest` `wedding_priest` `funeral_priest`
`schedule_editor` `bookings_manager` `staff_manager` `psalter_manager`

### Маршрутизация треб (extra_roles в priests)
`baptism_responsible` `wedding_responsible` `funeral_responsible`
`services_moderator` `offsite_responsible`

## 📊 СОСТОЯНИЕ БАЗЫ (на 2026-05-29)
**37 профилей** в системе:
- Клир: abbot(1), priest(3), deacon(1), altarnik(2), senior_altarnik(1) = 8
- Администрация: admin(1), developer(1), moderator(1), secretary(1) = 4
- Хор: regent(1) = 1
- Службы: florist(2), candle_seller(4), cleaner(6), cook(2), security(4), it_staff(1) = 19
- Без роли: staff(5) = 5

## 🗃 SUPABASE — ТАБЛИЦЫ
- `task_acks(id, task_id uuid → tasks, acknowledger_id text, acked_at, UNIQUE(task_id, acknowledger_id))` — идемпотентность групповых поручений
- `priests.duty_queue_index INT DEFAULT 0` — счётчик очереди бесед перед венчанием

## 🔐 ФОРМАТ ДОСТУПОВ ДЛЯ СОТРУДНИКОВ
- **Email (логин)**: `[10 цифр телефона]@khram-spas.ru`
- **Пароль**: те же 10 цифр телефона
- **TG-код**: 6-символьный hex → написать @HramSpasBot

## ✅ ПОСЛЕДНЯЯ СЕССИЯ
*Дата: 2026-06-14 (сессия 37)*

### Сессия 37: Полировка страницы крещения + OfficeView «Просроч.» + шапка BookingsModal

**Что сделали:**

1. **OfficeView.tsx — фильтр «⚠ Просроч.»** — добавлена кнопка в ряд фильтров статуса. Показывает `pending`/`confirmed` записи с датой < сегодня. Красный стиль (`bg-red-600`). `upcomingCount` исправлен: считает только `> today` (было `>=`).

2. **BookingsModal.tsx — убрана логика showPast** — ошибочно добавленная в сессии 36 кнопка «⚠ Просроч.» и state `showPast` удалены. Прихожанский раздел вернулся к исходному поведению (просроченные не показываются).

3. **BookingsModal.tsx — редизайн шапки** — двухстрочная шапка: первая строка (заголовок `text-xs` серый + кнопка ×), вторая строка (кнопки «С выполн.» + «Список/Календарь»). Больше не давит в одну строку.

4. **BookingView.tsx — крещение, финальная полировка:**
   - Символ Веры полностью (12 членов) с церковнославянскими ударениями из HTML-эталона
   - «О Таинстве Крещения» — 5 абзацев (Для чего / Что даёт / Это творит вода / Какими быть / Крещение детей)
   - Нумерация списка «Что нужно»: 1, 2, 3, 6, 7 (из эталона)
   - «Ксерокопии» вместо «Копии»; «Изучить» вместо «Выучить» для Символа Веры
   - «…а также пригласить хор и фотографа» в тексте про украшение купели
   - Форма записи (имя/телефон/кнопка) удалена — запись только через лавочку
   - Фото реальной украшенной купели: `public/kupel.jpg` (скопировано с рабочего стола)
   - Иконки: 🕐 и 👥 → золотые SVG (часы + силуэт)
   - Заголовок: крупный «🕊 Крещение» + под ним серый «ВСТУПЛЕНИЕ В ЦЕРКОВЬ ХРИСТОВУ»
   - «Ближайшие даты огласительных бесед» — золотой SVG-календарик перед подписью
   - Показывается ровно 6 дат (`.slice(0, 6)`)

**Commits:** `6fc4329`, `322c422`, `23126b3`, `34d4ec6`, `a55670f`, `bb05cfa`, `bc683e6`
**Деплой:** `railway up --detach` ✅ (7 деплоев)

---

### Сессия 36: Записи прихожан + беседа перед венчанием + редизайн крещения

**Что сделали:**

1. **loadDutySlots до 2 месяцев** — в `book/page.tsx` изменён диапазон загрузки дежурств с 2 недель на 2 месяца. Теперь `wedding_slots`, `confession_form`, `service_form` видят даты за 2 месяца вперёд, а не только за 2 недели.

2. **Священник беседы перед венчанием** — `createWeddingTalkFromDuty` теперь принимает `duty_priest_id` и `duty_priest_title`. Берёт telegram_chat_id / max_chat_id напрямую из БД по ID священника. Убрана логика `getNextWeddingCounselor()` (очередь) — теперь уведомляется конкретный дежурный.

3. **Текст подтверждения** — в done-шаге `wedding`: показывается дата/время из `selectedWeddingDuty` + `selectedServiceTime`, телефон +7 (921) 341-88-88 (лавочка). Убран `abbotPhone` из done-шага полностью (показывается для всех разделов один номер лавочки).

4. **Записи прихожан — Просроченные**: 
   - Добавлен state `showPast`
   - Bookings разбиваются на `futureBookings` (дата ≥ сегодня) и `pastBookings` (дата < сегодня)
   - По умолчанию отображаются только актуальные
   - Кнопка «⚠ Просроч. (N)» с `animate-pulse` появляется если есть неотработанные прошлые
   - Клик — переключает список в просроченные (бордовый цвет активного состояния)

5. **Календарь для всех ролей** — убран `CALENDAR_ROLES`, все сотрудники видят переключатель Список/Календарь. `canDrag` оставлен только для admin/abbot/moderator/developer/schedule_editor.

6. **Редизайн страницы крещения** — `baptism_form` в BookingView полностью переписан:
   - "Перед крещением" — четверги 18:50, родители+крёстные
   - Ближайшие даты как чипы (кнопки выбора), подтверждение выбора зелёным
   - "О Таинстве Крещения" — раскрываемый блок (`baptismShowSacrament`)
   - "Что нужно" — список с номерами, Символ Веры раскрывается (`baptismShowCreed`)
   - "Украшение купели"
   - Форма записи с кнопкой "Записаться на [дата]"

**TypeScript:** 0 ошибок | **Деплой:** `railway up --detach` ✅

---

### Сессия 35: Динамические слоты по дежурству

**Что сделали:**

1. **`loadDutyTimeSlots` в bookings.ts** — новая server action. Принимает `(date, period_start, period_end)`, генерирует 30-мин слоты (HH:00 и HH:30) внутри периода дежурства. Загружает существующие записи (`bookings` на эту дату, статус pending/confirmed), определяет `avail_full` (60 мин, без конфликтов в диапазоне [HH:00, HH:00+60мин)) и `avail_half` (30 мин).

2. **BookingView.tsx — 5 новых state** + хелпер `loadDutyTimesForSlot(duty)`:
   - `dutyExpandedDate` — какой день раскрыт
   - `dutyTimes: DutyTimeSlot[]` — загруженные слоты
   - `dutyTimesLoading` — спиннер
   - `selectedServiceTime: string | null` — выбранное HH:MM
   - `selectedServiceDuty: DutySlot | null` — выбранный день

3. **`wedding_slots` переделан** — клик по дню дежурства теперь раскрывает inline-блок с кнопками HH:00 (`avail_full`, 60 мин). Клик по времени → `setSelectedWeddingDuty` + `setSelectedServiceTime` + переход в `wedding_form`. В форме `periodLabel` показывает точное время (было «10:00–14:00»).

4. **`confession_form` переделана** — убраны `<input type="date">` и радиокнопки «Удобное время». Вместо них: список дней дежурства из `initialSlots` (ближайшие 2 мес), клик по дню → раскрытие с кнопками HH:MM (`avail_half`). Submit теперь передаёт `scheduled_time` в `createConfessionBooking`.

5. **`service_form` переделана** — то же самое для молебна / панихиды / освящения машины. Выбор дня дежурства → точное время (30 мин). Submit передаёт `scheduled_time` в `createTempleBooking`.

6. **Отец Серафим в Railway** — `doines48@gmail.com` приглашён через GraphQL mutation `projectInviteUser` с токеном из `~/.railway/config.json`. Railway CLI команды `invite` не существует.

**TypeScript:** 0 ошибок | **Деплой:** `railway up --detach` ✅

---

### Сессия 34: Предвенчальная беседа — полный редизайн раздела Венчание

**Что сделали:**

1. **wedding_slots переделан** — вместо выбора дат самого венчания теперь показываются слоты беседы (`rite_type = wedding_talk`) на ближайшие 2 недели. Текст изменён: «Перед венчанием проводится индивидуальная беседа жениха и невесты со священником». Убрано деление на Большой/Малый храм — просто список времён.

2. **wedding_form расширена** — новые поля: жених (ФИО, дата рождения, вероисповедание, № брака), невеста (то же), свидетельство о браке (серия/номер, место, дата — необязательно), желаемая дата венчания (select из канонических дат на 6 месяцев), пожелания (хор, цветочное оформление, **фотограф храма** — новое).

3. **createWeddingTalkBooking** — новая функция в bookings.ts: атомарно занимает слот, создаёт запись `booking_type = wedding_talk`, все данные в `notes` структурированным текстом, назначает дежурного священника через очередь, уведомляет священника и wedding_responsible с полными данными.

4. **SlotsModal** — badge «✦ Беседа» на слотах `wedding_talk`, кнопка ✦ на любом `individual` слоте переключает `rite_type` в `wedding_talk`/`null`.

5. **candle_seller** — добавлены `schedule_view: true`, `bookings_view: true` в role-defaults.ts.

**Commit:** `c314771` | **Деплой:** Railway (через `railway up --detach`, GitHub недоступен)

**⚠️ Нужно сделать после деплоя:** в SlotsModal пометить несколько individual-слотов ближайших 2 недель как `wedding_talk` (кнопка ✦), иначе список беседы будет пуст.

---

### Сессия 33: Фиксы от отца Серафима — возврат из выполненных, настоятель, календарь

**Что сделали:**

1. **Исправлен church_calendar** — 8 сентября 2026 (Сретение Владимирской иконы Пресвятой Богородицы) исправлено с `great_feast` на `weekday`: не двунадесятый праздник, только SQL UPDATE в БД.

2. **Настоятель в списке священников** — иерей Серафим Дейнэс имел `is_active = false` в таблице `priests` → исправлено на `true`. Теперь появляется в выпадающем списке «+ Назначить священника» во всех типах треб.

3. **Кнопка «↩️ Вернуть в работу»** — была скрытой мелкой ссылкой, возвращала в `pending`. Переделана в полноценную кнопку с рамкой, возвращает в статус `confirmed`. Видна на карточке в режиме «С выполн.».

4. **Взаимодействие с записями в календаре** — нажатие на карточку требы в виде «Календарь» открывает overlay-панель с полной карточкой: назначить священника, сменить статус, вернуть из выполненных, галочка беседы/катехизиса. После изменения — `refreshSignal` заставляет календарь перезагрузить данные.

**Commit:** `1e823b4` | **Деплой:** Railway (через GitHub push)

---

### Сессия 32: Визуальные записки + карточки дней + бот-меню поминовений

**Что сделали:**

1. **Визуальные записки в /book** — форма записки переделана под вид бумажной церковной записки:
   - Православный крест SVG (трёхбарный) в верхней части карточки
   - «О здравии» = красный крест + красный заголовок «О ЗДРАВИИ»
   - «О упокоении» / «Панихида» = чёрный крест + чёрный заголовок
   - Остальные типы = тёмно-зелёный крест
   - Имена вводятся на «линиях» с номерами (как на бумаге), лимит уменьшен с 20 до 10

2. **Карточки дней в расписании /book** — каждый день теперь в отдельной карточке:
   - Будний день → тёмно-зелёная шапка (`bg-[#2D5A1B]/8`, цвет храма)
   - Воскресенье → бордовая шапка (`bg-bordeaux/15`)
   - Пасха/Двунадесятые → тёмный бордовый + текст ivory
   - Великий праздник (вручную) → розовая шапка
   - Постный период → фиолетовая шапка + текст «Идёт Петров пост» / «Идёт Великий пост»
   - Память дня (святые) — строка под шапкой в соответствующем цвете

3. **Бот-меню поминовений** — команда `/pominovenie` в Telegram и Max:
   - Доступна только клиру и администрации (проверка роли)
   - Кнопки: О здравии / О упокоении / Сорокоуст / Годовое / Молебен / Панихида / Все записки
   - При нажатии — сгруппированный список имён из pending записок + счётчик
   - Добавлено в оба вебхука: `app/api/telegram/webhook/route.ts` + `app/api/max/webhook/route.ts`

**Commit:** `0b5ef32` | **Деплой:** Railway (через GitHub push)

---

### Сессия 31: Православный календарь — полный цикл правок

**Что сделали:**

1. **Воскресные дни в сетке календаря** — заголовок «Вс» и все даты воскресенья окрашены в бордовый текст (`text-bordeaux`). Без фона и рамок. Если воскресенье — праздник, берётся стиль праздника. **Commit:** `2a16d1a`

2. **Синхронизация внутреннего и внешнего контура** — запись из /office (без `rite_slot_id`) теперь блокирует соответствующий слот в /book. В `loadAvailableRiteSlots` добавлена проверка `bookings` где `rite_slot_id IS NULL AND location IS NOT NULL AND status IN ('pending', 'confirmed')` — совпадение по дате/времени/храму → `booked_count = capacity` (показывается занято). **Commit:** `3a8915b`

3. **Точка поста на великих праздниках** — фиолетовая точка теперь показывается и в ячейках с розовой рамкой (великий праздник вручную). На бордовом фоне (Пасха/Двунадесятые) — точка светлее (`bg-violet-200/80`), на белом/розовом — стандартная (`bg-violet-400/70`). Легенда не изменялась. **Commit:** `87aa152`, `b976251`

4. **Исправлены праздники 2026** — Воздвижение (27 сент) и Введение (4 дек) имели `day_type = 'weekday'` вместо `'great_feast'`. Исправлено SQL UPDATE через Management API.

5. **Заполнен 2027 год** — все 13 Двунадесятых праздников и Пасха (2 мая) вставлены в `church_calendar` с `day_type = 'great_feast'` и корректными именами.

6. **Расширены паттерны CalendarModal** — добавлены короткие варианты совпадающие с реальными именами в БД: `'Успение Пресвятой Владычицы'`, `'Рождество Господа'`, `'Сретение'`, `'Преображение'`, `'Воздвижение'`, `'Введение (Вход)'` и другие. **Commit:** `46dcfa4`

**Commits этой сессии:** `2a16d1a`, `3a8915b`, `87aa152`, `46dcfa4`, `b976251` | **Деплой:** Railway

---

### Сессия 30: Иерархия яркости цветов

**Что сделали:**

1. **Исправлена иерархия цветов** — великий праздник (вручную) выглядел ярче воскресенья, создавая впечатление большей важности.

   Правильная иерархия:
   - **Пасха / Двунадесятые** → полный тёмный bordeaux, текст белый
   - **Воскресенье** → bordeaux/20 (средний, заметно бордовый)
   - **Великий праздник (вручную)** → bg-rose-50 (бледный розовый, ниже воскресенья)

   Изменено в ScheduleView.tsx и BookingView.tsx. **Commit:** `667d4a1`

### Сессия 29: Правки от отца Владислава — цвета, значки, баги

**Что сделали:**

1. **Доступ admin к «Великий праздник»** — отец Владислав (admin) не видел кнопку «🌸 Отметить как Великий праздник». Добавлен `admin` в `canManageFeasts` в ScheduleView.tsx. **Commit:** `f432b30`

2. **Убран значок 👤 X/Y** с карточек богослужений — показывал `assignments.length / required_priests_min`, смысла не несёт. Название службы теперь занимает всю строку. **Commit:** `1b0624c`

3. **Исправлено сохранение великих праздников** — `setCustomFeast` делал `upsert` без UNIQUE constraint на `date`, создавал дубли. Заменено на `UPDATE ... WHERE date = ?` с обработкой ошибки. **Commit:** `1b0624c`

4. **Постные дни в сетке православного календаря** — фиолетовая точка в правом верхнем углу ячейки. Добавлена в легенду. **Commit:** `1b0624c`

5. **Цвета в /book (публичное расписание):**
   - Баннер с названием праздника (память дня) красится в цвет дня, не всегда зелёный
   - Если великий праздник + постный день → цвет праздника, метка «🕊 пост» текстом справа
   - Убран цвет для обычной субботы (без праздника = как будний)
   - **Commit:** `ae2af25`, `49deb7b`

6. **Цвета в служебном расписании (ScheduleView):**
   - Загружается `loadCalendarRange` для текущей недели
   - Воскресенье → фон bordeaux/10, текст bordeaux
   - Пасха/Двунадесятые → фон bordeaux (тёмный), текст ivory
   - Великий праздник → розовый фон
   - Постный день → метка «пост» рядом с датой
   - **Commit:** `49deb7b`

7. **Цвет bordeaux обновлён** — с `#6B1E2A` на `#6f0028` (выбор отца Владислава). Применяется глобально. **Commit:** `7f9b49a`

**Commits этой сессии:** `f432b30`, `1b0624c`, `ae2af25`, `49deb7b`, `7f9b49a` | **Деплой:** Railway

### Сессия 28: Навигация по дате + цвета праздников (ответы отца Владислава)

**Что сделали:**

1. **Переход из православного календаря на расписание** — в CalendarModal добавлена кнопка «📅 Перейти на эту неделю →». При нажатии — расписание сотрудников переключается на нужную неделю без перезагрузки. Prop `onGoToWeek` в CalendarModal → `setCurrentDate(parseISO(date))` в ScheduleView. **Commit:** `e1b4275`

2. **Цвета праздников — новая схема по ответу отца Владислава:**
   - **Пасха + Двунадесятые (12)** → красный (bordeaux) — автоматически по паттерн-матчингу имени
   - **Великие праздники** (Казанская, Донская, Покров, особо чтимые) → пастельный розовый — **вручную**, только настоятель и модератор
   - **Постные дни** — без изменений (фиолетовая полоска)
   - Убран старый автоматический золотой цвет для «великих» (был неточный)

3. **SQL: колонка `is_custom_feast BOOLEAN DEFAULT false`** в таблице `church_calendar` — добавлена через Management API

4. **Actions в schedule.ts:**
   - `setCustomFeast(date, value)` — сохранить/снять отметку великого праздника
   - `loadCalendarRange(from, to)` — загрузка church_calendar для /book
   - `loadUpcomingPublicServices` — добавлен `is_custom_feast` в SELECT

5. **CalendarModal** — полностью переписан:
   - Кнопка «🌸 Отметить как Великий праздник» (только для `canManageFeasts`)
   - Оптимистичное обновление (состояние меняется мгновенно, потом сохраняется в БД)
   - Легенда: «Пасха и двунадесятые» (bordeaux) + «Великие праздники» (rose)

6. **BookingView (/book для прихожан)** — те же цвета:
   - `getDayLevel()` обновлён: проверяет `is_custom_feast` из calendarMap и из bogослужений
   - Новый prop `calendarDays` — загружается в /book page отдельно
   - `useMemo` для calendarMap

**Commits:** `e1b4275`, `14ff300` | **Деплой:** Railway (в процессе)

**Открытые вопросы:**
- Ждём **реквизиты Тинькофф / QR СБП** для пожертвований в форме записок

### Сессия 26: Обратная связь от Настоятеля — цвета, записки, кабинет

**Что сделали:**

1. **Цвета праздников исправлены** — воскресенье и двунадесятые Господские теперь бордовый (было синий), великие святые — зелёный (было золотой), богородичные — голубой (без изменений). Пасха — особый бордовый.

2. **Постные дни** — добавлена функция `isFastingDay()` в `lib/orthodox-calendar.ts`. Среда и пятница подсвечиваются фиолетовым фоном + метка «пост». Сплошные седмицы (Святки, Мытарей, Масленица, Пасхальная, Троицкая) — не постные.

3. **Записки расширены до 7 типов** — О здравии, О упокоении, Сорокоуст (40 дней), Годовое поминовение, Молебен, Панихида, Особое прошение (5 видов: о болящем, доброго дела, благодарение, о семье, иное). Добавлен выбор даты подачи.

4. **church_notes: получатели изменены** — теперь уведомляется алтарный блок: abbot, priest, deacon, altarnik, senior_altarnik (раньше: deacon + admin + moderator).

5. **SQL-миграция church_notes** — добавлены колонки `desired_date DATE`, `submitter_phone TEXT`, `petition_type TEXT`.

6. **Алтарная рассылка в cron** — раздел 9 в `app/api/cron/reminders/route.ts`: накануне Литургии алтарный блок получает сводку всех pending записок, сгруппированных по типу.

7. **«Кабинет прихожанина»** — переименован везде: /book, /cabinet, /cabinet/login.

**Commit:** `a489682` | **Деплой:** Railway (в процессе)

**Открытые вопросы:**
- Ждём ответа от **отца Владислава** по логике цветов (вопросы переданы Александром)
- Ждём **реквизиты Тинькофф / QR СБП** для пожертвований в форме записок

### Сессия 25: Улучшения /book — цвета, посты, записки, личный кабинет

**Что сделали:**

1. **Цвета дней в расписании** — `getDayLevel()` в BookingView.tsx: воскресенье (синий фон), суббота (лёгкий синий), Пасха (бордовый), Господские двунадесятые (тёмно-синий), Богородичные (голубой), Великие праздники (золотой). Цветная точка + фон у заголовка дня.

2. **Баннер поста** — `getCurrentFast(date)` в lib/orthodox-calendar.ts; баннер 🕯 вверху меню /book с названием, датой окончания и уставной строгостью.

3. **Записки** — новый аккордеон-блок 🕯 в /book («О здравии» / «О упокоении»), форма с динамическим списком имён, таблица `church_notes` в Supabase, уведомление abbot+deacon+moderator+admin; `app/actions/church-notes.ts`.

4. **Личный кабинет /cabinet** — полноценная система:
   - Таблицы: `parishioner_profiles`, `parishioner_sessions`, `parishioner_otp`
   - `app/actions/parishioner-auth.ts` — requestOtp / verifyOtp / getCurrentParishioner / logout / TG-link
   - `/cabinet/login` — ввод телефона → OTP в TG/Max или код на экране (первый вход)
   - `/cabinet` — 3 вкладки: Записи / Записки / Профиль
   - TG webhook: `/start P_TOKEN` → привязка кабинета; то же в Max webhook
   - Кнопка «👤 Личный кабинет» на /book

**Новые файлы:**
- `app/actions/church-notes.ts` — createChurchNote
- `app/actions/parishioner-auth.ts` — весь auth для прихожан
- `app/cabinet/login/page.tsx` + `app/cabinet/page.tsx`
- `components/cabinet/CabinetLoginView.tsx` + `CabinetDashboard.tsx`

**Commit:** `eb9239e` | **Деплой:** Railway (в процессе)

### Сессия 24: Причина блокировки слота + тест-роли

**Что сделали:**

1. **SQL: колонка `block_reason TEXT`** в таблице `rite_slots`

2. **Замочек с причиной (SlotsModal)** — нажатие 🔒 теперь не блокирует сразу, а показывает inline-форму с полем ввода «Причина» (необязательное: Молебен / Крестный ход / Договорённость...) + кнопки «Закрыть слот» / «Отмена». Причина отображается серым текстом под меткой 🔒 Закрыт в панели администратора.

3. **Заблокированные слоты в /book** — убран фильтр `is_blocked=false` из `loadAvailableRiteSlots`. Заблокированные слоты теперь видны прихожанам серым, некликабельным блоком с текстом «Запись недоступна» (без указания причины). Работает в секциях крещения, венчания, отпевания. `availableCount` и счётчик мест считается правильно (блокированные не учитываются).

4. **Страница /admin/test-roles** — переключение тест-аккаунта (79111888877@khram-spas.ru / пароль 79111888877) между любыми ролями одним кликом. Ссылка «Тест-роли» добавлена в навигацию расписания (desktop + мобильная) для ролей abbot/admin/developer. После переключения роли: открыть инкогнито → войти как тест-аккаунт → видеть систему глазами этого сотрудника.

**Новые файлы:**
- `app/actions/test-roles.ts` — getTestAccount, setTestAccountRole
- `app/admin/test-roles/page.tsx` — серверная страница
- `components/admin/TestRolesView.tsx` — клиентский компонент с сеткой ролей

**Commit:** `5a815bd` | **Деплой:** Railway `4884b623`

### Сессия 23: Разбор функции замочка в слотах

**Что сделали:**

1. **Разобрали функцию замочка (🔒) в SlotsModal** — подтверждено: замочек устанавливает `is_blocked = true` в таблице `rite_slots`. Закрытый слот не виден прихожанам в /book и недоступен для записи. Открывается повторным нажатием.

2. **Зафиксировали формат ответов для khram-spas** — все планы и отчёты теперь только в блоке кода (Telegram-формат), чтобы можно было скопировать напрямую в TG.

**Открытый вопрос по слотам:**
- Нужно ли добавить поле «Причина закрытия» к слоту? (например: «Молебен», «Мероприятие») — ждёт ответа.

### Сессия 22: Форма богослужения + редизайн православного календаря

**Что сделали:**

1. **БАГ saveService исправлен** — добавлена обработка ошибок в UPDATE и INSERT ветках `saveService`. Теперь при любой ошибке Supabase показывается красный блок с текстом прямо в форме. Раньше ошибка молча проглатывалась.

2. **Убраны 3 роли из формы клира** — `panikhida`, `moleben`, `duty_after` удалены из `ALL_ROLES` в `ServiceModal.tsx`. Осталось 7 кнопок: Возглавляет, Сослужит, Проповедь, Исповедует, Требный (б. храм), Требный (м. храм), Дежурный.

3. **Чекбокс «Не отображать в публичном расписании»** — добавлен в форму создания/редактирования богослужения. Скрытые службы (`is_staff_only = true`) не попадают в /book (прихожане не видят). Сотрудники видят всё.
   - SQL: `ALTER TABLE services ADD COLUMN is_staff_only BOOLEAN NOT NULL DEFAULT false`
   - Фильтр добавлен в `loadUpcomingPublicServices` (`.eq('is_staff_only', false)`)

4. **Православный календарь — авто-открытие сегодня** — при открытии модалки текущего месяца сразу показывается информация о сегодняшнем дне (раньше нужно было тапнуть).

5. **Убран раздел «Чтения дня»** — блок Апостол/Евангелие, кнопка «⬇ Чтения» и вся логика обогащения полностью удалены из `CalendarModal.tsx`.

6. **Новые цвета праздников в календарной сетке:**
   - Пасха → бордовый (`bg-bordeaux`)
   - Двунадесятые → тёмно-синий (`bg-navy`)
   - Великие праздники → золотой (`bg-gold`)
   - Малые праздники → убрано яркое выделение (только маленькая точка)
   - Определение уровня праздника через паттерн-матчинг по `name` (без SQL)

**Commit:** `902d60c` | **Деплой:** Railway `d903b643`

### Сессия 21: Обновление CRON_SECRET и calendar-import cron

**Что сделали:**

1. **CRON_SECRET обновлён в Railway Variables** — заменён слабый `khram_spas_cron_2026` на длинный хэш из `~/.claude/secrets/khram-spas.env`

2. **Все 3 cron-сервиса обновлены** через Railway GraphQL API:
   - `reminders-cron` — новый секрет в curl-команде
   - `task-overdue-cron` — новый секрет в curl-команде
   - `backup-cron` — новый секрет + добавлен вызов `/api/cron/calendar-import` с условием `[ $(date +%d) = 25 ]`

3. **Calendar-import теперь запускается 25-го числа в 2:15 ночи** (вместе с backup-cron) — отдельный сервис создать нельзя (лимит плана), функционально эквивалентно

### Сессия 20: Умный парсер памяти святых (структурированные данные)

**Что сделали:**

1. **Переписан парсер памяти святых** (`app/actions/calendar-notes.ts`):
   - Старый парсер: брал `<meta og:description>` — плоский текст без структуры
   - Новый парсер: разбирает HTML структуру azbyka.ru/days/YYYY-MM-DD
   - **Три уровня приоритета:**
     - Седмица — из `<div class="lc">…</div>` (зелёная рамка на сайте)
     - Главный праздник — из `<ul class="paragraph-0">`, каждый `<li>` отдельно (кроме ideograph-3 чтобы не дублировать)
     - Особо чтимые — из `<li class="ideograph-3">` (красные на сайте)
     - Fallback — первый обычный `<li>` если нет ни праздника ни особо чтимых
   - Добавлена вспомогательная `stripHtml()`: убирает span'ы, теги, HTML-сущности, пробелы перед пунктуацией
   - Добавлен параметр `?force=true` в cron route для ручной перезаливки (обходит idempotency)
   - **Commits:** `fee1be9`, `e99511f`, `f2420a0`, `cec49ac`

2. **Данные перезалиты в БД:**
   - Июнь (30 дней) и июль (31 день) — черновики `is_published=false`
   - Пример 1 июня: «Седмица 1-я по Пятидесятнице. Троицкая седмица. День Святого Духа. блгв. вел. кн. Димитрия Донского и прп. Евфросинии, вел. кн. Московской»
   - Пример 2 июня: «…Мчч. Фалалея, Александра и Астерия. Обретение мощей свт. Алексия, митрополита Киевского…»

**Не сделано / технический долг:**
- Debug-endpoint `/api/cron/calendar-test` остался в коде — можно удалить
- CRON_SECRET в Railway Variables — старый (`khram_spas_cron_2026`), не совпадает с `~/.claude/secrets/khram-spas.env`

### Сессия 19: Память святых + защита от гонки слотов

**Что сделали:**

1. **Память святых — загрузка июня и июля 2026** (`app/actions/calendar-notes.ts`):
   - Заменён источник: `calendar.patriarchia.ru` → `azbyka.ru/days/YYYY-MM-DD`
   - Добавлены параметры `?year=&month=` в cron-endpoint для ручного запуска
   - **Commits:** `3b67b29`, `0fc9b40`, `3a936ce`

2. **Защита от гонки при бронировании слотов** (`app/actions/slots.ts`, `components/booking/BookingView.tsx`):
   - Сервер: оптимистичная блокировка — `UPDATE ... WHERE booked_count = N`
   - Клиент: при `already_taken` — слот исчезает из `liveSlots` без ошибки
   - **Commit:** `b8d64a1`

### Сессия 18: Исправление /office — выполнено

1. SQL-миграция: 6 новых значений в enum `booking_type` (`wedding_talk`, `baptism_talk`, `last_rites`, `communion_home`, `hospital`, `baptism_general`)
2. Общее крещение в `/office`: время 13:00 фиксировано, счётчик мест (12 max), `getGeneralBaptismCount(date)`
3. Кнопка «← Назад» в /office → /dashboard

**Деплой:** commit `80d0e53`

## ➡️ СЛЕДУЮЩИЙ ШАГ

### 🔴 ПРИОРИТЕТ: Перенос на российский хостинг (сессия 40, план согласован)

**Причины:** ФЗ-152 (персональные данные) + работа без VPN

**Итоговый стек:**
| Сейчас | После |
|--------|-------|
| Railway (США) | Amvera (Россия) |
| Supabase (Германия) | Timeweb Cloud Managed PostgreSQL (Россия) |
| — | Домен на reg.ru или nic.ru |
| Supabase Auth | Кастомный JWT (OTP через TG/Max — без изменений) |

**Этапы:**
1. Зарегистрировать домен на reg.ru — **ждём выбора имени от Александра**
2. Завести Timeweb Cloud Managed PostgreSQL
3. Завести Amvera, подключить GitHub
4. Перенести базу (pg_dump → pg_restore)
5. Переписать авторизацию: Supabase Auth → кастомный JWT + drizzle-orm (3-5 дней)
6. Деплой на Amvera + привязать домен
7. Обновить вебхуки Telegram и Max
8. Тестирование всех ролей

**Оценка:** 1,5-2 недели | **Стоимость:** ~800-1400 руб/мес

**Открытый вопрос:** Какое доменное имя хочешь?

---

### После деплоя (сессия 39) — проверить на сайте:
1. /book → «Крещение» → даты НЕ кликабельны (просто отображаются), внизу текст «После беседы — запись в лавке: +7 (921) 341-88-88»
2. /office → создать запись «Крещение» → появляются чекбоксы Хор / Украшение купели / Фотограф
3. Подтвердить крещение с Хором → регент получает уведомление; без Хора — нет

### Открытые вопросы (продукт):
- **Фотограф храма** — кто получает уведомление? (пока не реализовано — нет профиля в системе)
- **Реквизиты Тинькофф / QR СБП** для пожертвований в форме записок
- Синодики для Сорокоуста и Годового поминовения — хранить отдельно, напоминать автоматически
- Свечница: `/start [код]` боту @id7813204684_bot
- Ответ от отца Серафима: при клике в календаре открывать модалку или переключать в список?

---

## ✅ ПОСЛЕДНЯЯ СЕССИЯ
*Дата: 2026-06-16 (сессия 39)*

### Сессия 39: Редизайн логики Крещения — /book только информация, /office управляет записью

**Что сделали:**

1. **/book → Крещение** — даты огласительных бесед теперь только отображаются (не кликабельны), убрана возможность «выбрать дату», убрана строка «✓ Выбрано». Желтое предупреждение заменено на: «После огласительной беседы запишитесь на Крещение в лавочке храма. Тел: +7 (921) 341-88-88». Запись на крещение теперь только через лавку (внутренний контур).

2. **/office → форма крещения** — при выборе типа «Крещение (индивидуальное)» появляется блок «Дополнительно» с тремя чекбоксами: 🎶 Хор / 🌸 Украшение купели / 📷 Фотограф храма. Сбрасываются при смене типа.

3. **office.ts** — `createOfficeBooking` принимает `choir_requested`, `decor_requested`, `photographer_requested` и сохраняет в БД. `updateOfficeBookingStatus` при переходе в `confirmed` + тип `baptism` отправляет уведомления: флористу если `decor_requested`, регенту если `choir_requested`.

4. **bookings.ts** — `sendDecorationNotification` и `sendChoirNotification` проверяют флаги для крещения: не отправляют уведомление если `baptism` и флаг не выставлен. Венчание и отпевание — без изменений (всегда уведомляют).

5. **Supabase** — добавлена колонка `photographer_requested BOOLEAN DEFAULT false` в таблицу `bookings`.

**Commit:** `882e297` | **Деплой:** `railway up --detach` ✅

---

### Сессия 38: Автосинхронизация handoff между Александром и отцом Серафимом

**Что сделали:**

1. **Репо GitHub публичное** — `github.com/skvortsovproduction2020-sudo/claude-config` открыт для чтения без авторизации

2. **hooks/handoff-sync.js** — PostToolUse хук на машине Александра: при каждом сохранении любого handoff-файла автоматически делает `git add + commit + push` на GitHub

3. **hooks/handoff-pull.js** — SessionStart хук для отца Серафима: при каждом старте Claude Code делает `git fetch + checkout handoff/` с GitHub

4. **setup-serafim-mac.sh** — установочный скрипт для Mac (bash). Запускается одной командой curl. Исправлен баг с путём команды в settings.json (Python adjacent string concatenation)

5. **Синхронизация протестирована и работает** — тестовая строка добавлена на машине Александра, появилась у отца Серафима после перезапуска Claude Code ✅

**Машина отца Серафима:**
- Mac, username `seraphimdeines`, путь `/Users/seraphimdeines/.claude/`
- Node.js: `/usr/local/bin/node`
- Хук настроен в `~/.claude/settings.json`
